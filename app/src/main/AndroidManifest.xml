<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.itpn.findyourbooks">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FindYourBooks">
        <activity android:name=".Tienda"></activity>
        <activity android:name=".IniciaSesionPersona" />
        <activity
            android:name=".ui.login.IniciaSesionPersona"
            android:label="@string/title_activity_inicia_sesion_persona" />
        <activity android:name=".pantallaPrincipalPersona" />
        <activity android:name=".RegisterStore" />
        <activity android:name=".RegisterPerson" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>